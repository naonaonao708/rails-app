default: &default
  adapter: postgresql
  encoding: unicode
  host: db
  user: postgresql
  port: 5432
  password: <%= ENV.fetch("DATABASE_PASSWORD") %>
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") {5} %>
